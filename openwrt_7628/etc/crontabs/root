* * * * * /bin/report_ip.sh

# Restart u3_service at 3:30am daily to prevent 4am failures
30 3 * * * /etc/init.d/u3_service restart

# Recovery watchdog — only if service is enabled and not running
*/5 * * * * [ -f /etc/rc.d/S99u3_service ] && { pgrep -f u3.sh >/dev/null 2>&1 || /etc/init.d/u3_service start; }

#* * * * * /bin/u3.sh
