[project]
name = "camera-calibration"
version = "0.1.0"
description = "Camera calibration tools and MQTT image viewer for AVI cameras"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "opencv-python",
    "paho-mqtt",
    "pyusb",
]
